# Copy this file to server/.env and fill the values before deploying to production
# Use sk_test_* keys for sandbox/testing and sk_* keys for production
PAGARME_API_KEY=sk_test_xxx
PAGARME_PUBLIC_API_KEY=pk_test_xxx
PORT=4000

# (Optional) If you prefer to use pre-created stable Payment Links instead of
# creating links at runtime, set these values to the Payment Link ID (pl_...)
# or the full URL for each plan. Recommended for production.
PAYMENTLINK_EXPERIENCE=pl_xxx_or_full_url
PAYMENTLINK_TRANSFORMATION=pl_xxx_or_full_url

# Webhook secret (set this value in the Pagar.me webhook configuration and here)
PAGARME_WEBHOOK_SECRET=whsec_xxx

# Meta / Facebook Conversions API (optional) — set these to enable server-side sends
# `META_PIXEL_ID` is your Pixel numeric id (ex: 1630651671697917)
# `META_ACCESS_TOKEN` is a long-lived access token created for the Pixel / Business
META_PIXEL_ID=
META_ACCESS_TOKEN=
META_API_VERSION=v17.0
